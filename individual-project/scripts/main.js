import { initNavigation } from './navigation.js';
import { displayFarmingPlaces } from './farmingPlaces.js';

document.addEventListener('DOMContentLoaded', () => {
    // Initialize navigation
    initNavigation();

    // Display farming places
    displayFarmingPlaces();
});
